# FastCut ✂️ (Идея взята из ТЗ Авито)

**FastCut** — это высокопроизводительный асинхронный сервис для сокращения ссылок (URL Shortener), написанный на Python с использованием современного стека технологий.

Проект реализует полный цикл работы с короткими ссылками: от создания и кастомных алиасов до быстрого редиректа с кэшированием и сбором статистики (в планах).

## 🚀 Функциональность

* **Сокращение ссылок**: Генерация коротких ссылок или создание собственных (кастомных) алиасов.
* **Моментальный редирект**: Использование Redis для кэширования "горячих" ссылок (время ответа < 5мс).
* **Аутентификация**: Регистрация и авторизация пользователей (JWT). Привязка ссылок к пользователю.
* **Асинхронность**: Полностью неблокирующий I/O благодаря FastAPI и asyncpg.
* **Docker-first**: Проект полностью контейнеризирован и готов к запуску одной командой.
* **Тесты**: Покрытие Unit, Integration и E2E тестами (Pytest).

## 🛠 Технологический стек

* **Язык**: Python 3.11+
* **Фреймворк**: FastAPI
* **База данных**: PostgreSQL 15 (драйвер `asyncpg`, ORM SQLAlchemy 2.0)
* **Кэширование**: Redis 7
* **Миграции**: Alembic
* **Деплой**: Docker & Docker Compose
* **Тестирование**: Pytest, HTTPX

## 📂 Структура проекта

```text
FastCut/
├── app/
│   ├── migrations/      # Миграции базы данных (Alembic)
│   ├── tests/           # Тесты (Unit, Integration, E2E)
│   ├── URLS/            # Логика работы со ссылками (Service, DAO, Router)
│   ├── Users/           # Логика работы с пользователями (Auth)
│   ├── config.py        # Настройки Pydantic
│   ├── database.py      # Подключение к БД и Redis
│   └── main.py          # Точка входа приложения
├── docker/              # Скрипты запуска (app.sh)
├── docker-compose.yml   # Конфигурация сервисов
├── Dockerfile           # Сборка образа приложения
└── pyproject.toml       # Зависимости
```
🔧 Установка и запуск
Предварительные требования

    Установленный Docker и Docker Compose.

1. Клонирование репозитория
```Bash

git clone [https://github.com/alonyaska/FastCut.git](https://github.com/alonyaska/FastCut.git)
cd FastCut
```
2. Настройка окружения

Создайте файл .env в корне проекта на основе примера.

Пример .env:
Фрагмент кода
```
MODE=DEV

# Настройки PostgreSQL
DB_HOST=db
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=fastcut_app

# Переменные для инициализации контейнера Postgres
POSTGRES_DB=fastcut_app
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Настройки Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Настройки приложения
SECRET_KEY=your_super_secret_key_here
ALGORITHM=HS256

# Настройки редиректа
REDIRECTED_HOST=127.0.0.1
REDIRECTED_PORT=8000
```

    ⚠️ Важно: Убедитесь, что DB_HOST=db и REDIS_HOST=redis при запуске через Docker.

3. Запуск через Docker Compose

Эта команда соберет образ, поднимет базу данных, Redis, применит миграции и запустит сервер.
Bash
```
docker-compose up --build
```
После запуска:

    API доступно по адресу: http://127.0.0.1:3333

    Документация (Swagger): http://127.0.0.1:3333/docs

🔌 Основные Эндпоинты
Метод	Путь	Описание
- POST	/auth/register	Регистрация нового пользователя
- POST	/auth/login	Вход и получение JWT токена
- POST	/links/shortURL	Создание короткой ссылки (требует Auth)
- GET	/{short_code}	Редирект на оригинальный URL

🧪 Тестирование

Проект покрыт тестами. Для запуска тестов внутри контейнера выполните:
```Bash

docker-compose exec fastcut pytest -v
```
или локально (требуется поднятая БД):
```Bash

pytest app/tests
```
👨‍💻 Автор

Alonyaska (Роман)

    GitHub: @alonyaska

Проект разработан в учебных целях для демонстрации навыков backend-разработки.
